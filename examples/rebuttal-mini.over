// A tiny version of the program submitted for rebuttal
assume fStr: (String) => Int;
assume fInt: (Int) => Int;
assume callStr: ∀(R). ((String) => R) => R;
assume callInt: ∀(R). ((Int) => R) => R;
assume compose: ∀(R). ((R) => R, (R) => R) => ((R) => R);

call<callInt, callStr>(
  compose(
    compose(
      a<fInt, fStr>,
      b<fInt, fStr>
    ),
    c<fInt, fStr>
  )
)
